import 'package:flutter/material.dart';

class SeriesScreenImage extends StatelessWidget {
  const SeriesScreenImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
